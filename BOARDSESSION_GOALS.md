# BoardSession 与 VS Code / Copilot Chat 集成目标

> 本文记录项目目标与约束，不代表当前代码已经实现或验证全部要求。以后审查和修改以此为准，避免为解决局部问题扩大原生代码改动范围。

## 1. 上游兼容与最小侵入

尽量不修改 VS Code 官方原生代码。优先以新增的 BoardSession 核心 npm 包和独立 VS Code 扩展适配层实现功能，使以后同步上游分支时的冲突、重做量和额外依赖最小。只有扩展接口确实无法实现且有明确必要时，才允许增加小而集中的原生改动。

## 2. 三部分职责及边界

1. **核心 npm 包**：`extensions/boardsession/board-session/`（实际包名和目录名以项目为准，当前包名为 `@carizon/board-session`）。负责利用 wolfSSH，经由任意 0～N 个跳板服务器（jump server）建立到板端的持久 SSH 会话，向调用方提供统一建链、执行命令、发送输入、获取输出及关闭能力。此部分已经基本完成并测试符合需求，**没有确认必要性不得修改核心实现**。
2. **VS Code 扩展适配层**：`extensions/boardsession/` 中核心 npm 包以外的代码。仅负责连接信息采集、连接状态与交互、注册面向 Copilot Chat 的 `board_terminal_*` 工具及调用核心包；不重新实现核心连接/跳板/终端协议。
3. **必要的 VS Code 工程接入**：仅在扩展机制不能满足的地方，对聊天界面、工具可见性/切换和构建入口做最小改动，不将 BoardSession 业务逻辑塞进通用工作台、工具服务或 Copilot 原生代码。

## 3. 聊天交互与终端工具切换

- 在 **Copilot Chat 会话输入栏的语音转文字按钮旁边**新增连接形状的按钮，而非只在整个 Chat 视图标题栏添加入口。
- 未激活时为灰色/普通态，后续 Copilot Chat 使用原生 `terminal_*` 工具。
- 点击激活：若已连接，直接切换；若未连接，在 **Copilot Chat 交互视图内**引导输入板子与跳板信息并建链。连接成功后按钮显示蓝色激活态，后续 Copilot Chat 中原生终端相关 tool call 应由 `board_terminal_*` 工具取代；不得在板端模式误执行到本机。
- 再点按钮：退出板端模式，恢复原生终端相关 tool call，按钮恢复未激活态。明确管理断链、取消输入与重连时的状态，不能显示已连接却实际不可用。

## 4. 连接信息输入和默认值

板子信息全部选填，留空时采用：
- IP/host：`192.168.2.62`
- user：`root`
- port：`22`
- X.509 复合 PEM 密钥文件：`extensions/boardsession/board-session/prebuilds/client-identity.pem`（以 npm 实际打包后的可解析相对路径为准）。

统一调试密钥应随核心 npm 包打包以便内部调试；它是私钥，不得误当作可公开分发的生产凭据。

跳板信息按实际连接顺序逐台输入：**IP/host → user → port → 密码**。
- IP/host 必须作为每台跳板的第一个输入；留空表示跳板列表结束，不再要求其他字段。
- IP/host 非空才继续要求其余字段；它们可留空，分别默认 `user=root`、`port=22`、`password=123456`。
- 支持 0～N 台跳板，不按跳数设计不同业务接口。

## 5. 构建与包的独立性

完整 VS Code 工程构建时，应先将核心 npm 包**独立构建并打包**，其他使用方通过外部 npm 包依赖接口接入，不从其源码或 `dist` 目录直接相对导入。尽量沿用现有扩展构建机制，不为单个功能引入繁重、耦合的全局构建步骤。确保产物实际包含核心包运行所需的 worker、原生模块、动态库与内部调试密钥，并明确平台限制。

## 6. 实施顺序

优先实现并验证完整功能闭环及模块边界，不做超出当前需求的扩展性设计；单元测试在功能稳定后按必要范围补充。已有核心 npm 包的可用行为应视为回归基线。

## 验收关注点

可直接连接板子，也可通过任意 1～N 台跳板连接；首次点击能按上述顺序和默认值采集参数；连接成功后按钮与 Copilot 工具集合一致，终端操作确实落在板端；再次点击恢复原生工具；取消、断线和失败不残留错误状态；完整构建能独立产出并引用 npm 包，且对上游 VS Code 的改动保持可审计的最小差异。
